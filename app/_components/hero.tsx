'use client';

import React from "react";

const Hero: React.FC = () => (
    <div className="w-full h-screen flex items-center justify-center">
        Hello world
    </div>
);

export default Hero;